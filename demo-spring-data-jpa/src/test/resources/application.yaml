spring:
  jpa:
    show-sql: true
  datasource:
    hikari:
      auto-commit: true
      jdbc-url: jdbc:h2:mem:demo_spring_data_jpa;MODE=MySQL;DB_CLOSE_DELAY=-1
      username: sa
      password:
      connection-test-query: select 'connection'
      driver-class-name: org.h2.Driver
  thymeleaf:
    cache: false
